<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.chachamaru</groupId>
        <artifactId>claude-harness-parent</artifactId>
        <version>4.0.0-java-SNAPSHOT</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <artifactId>java-harness-cli</artifactId>
    <packaging>jar</packaging>

    <name>Java Harness CLI Layer</name>
    <description>CLI layer - Command-line interface and user interaction</description>

    <dependencies>
        <!-- Shared module (constants and utilities) -->
        <dependency>
            <groupId>com.chachamaru</groupId>
            <artifactId>java-harness-shared</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- Foundation layer -->
        <dependency>
            <groupId>com.chachamaru</groupId>
            <artifactId>java-harness-foundation</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- Protocol layer -->
        <dependency>
            <groupId>com.chachamaru</groupId>
            <artifactId>java-harness-protocol</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- Security layer -->
        <dependency>
            <groupId>com.chachamaru</groupId>
            <artifactId>java-harness-security</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- Workflow layer -->
        <dependency>
            <groupId>com.chachamaru</groupId>
            <artifactId>java-harness-workflow</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- Collaboration layer -->
        <dependency>
            <groupId>com.chachamaru</groupId>
            <artifactId>java-harness-collaboration</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- Jackson for JSON processing -->
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
        </dependency>

        <!-- Logging -->
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
        </dependency>

        <!-- Picocli for CLI framework -->
        <dependency>
            <groupId>info.picocli</groupId>
            <artifactId>picocli</artifactId>
            <version>4.7.5</version>
        </dependency>

        <!-- GraalVM SDK for native image configuration (compile only) -->
        <dependency>
            <groupId>org.graalvm.sdk</groupId>
            <artifactId>graal-sdk</artifactId>
            <version>23.1.0</version>
            <scope>provided</scope>
        </dependency>

        <!-- Testing -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <!-- Native Image Profile -->
        <profile>
            <id>native</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.graalvm.buildtools</groupId>
                        <artifactId>native-maven-plugin</artifactId>
                        <version>0.10.0</version>
                        <extensions>true</extensions>
                        <configuration>
                            <imageName>harness</imageName>
                            <mainClass>com.chachamaru.harness.cli.HarnessCLI</mainClass>
                            <buildArgs>
                                <buildArg>--no-fallback</buildArg>
                                <buildArg>--initialize-at-build-time=java.time.LocalDateTime</buildArg>
                                <buildArg>--initialize-at-build-time=java.util.LinkedHashMap</buildArg>
                                <buildArg>--initialize-at-build-time=java.util.ArrayList</buildArg>
                                <buildArg>--initialize-at-build-time=java.util.HashMap</buildArg>
                                <buildArg>-H:+ReportExceptionStackTraces</buildArg>
                                <buildArg>-H:IncludeResources=.*\\.yaml$</buildArg>
                                <buildArg>-H:IncludeResources=.*\.json$</buildArg>
                                <buildArg>-H:IncludeResources=.*\\.yml$</buildArg>
                                <buildArg>-H:ConfigurationFileDirectories=src/main/resources/META-INF/native-image</buildArg>
                            </buildArgs>
                        </configuration>
                        <executions>
                            <execution>
                                <id>build-native</id>
                                <goals>
                                    <goal>compile-no-fork</goal>
                                </goals>
                                <phase>package</phase>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
